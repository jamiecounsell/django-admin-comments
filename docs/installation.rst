============
Installation
============

At the command line::

    $ easy_install django-admin-comments

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-admin-comments
    $ pip install django-admin-comments
